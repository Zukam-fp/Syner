<%= simple_form_for @match do |f| %>
  <%= f.input :address, collection: Match::ADDRESSES.map{|address| address[:name]}, prompt: 'Select a Futsal' %>
  <%= f.input :date, prompt: 'Select a date' %>
  <%= f.button :submit %>
 <% end %>

 <%= console %>
